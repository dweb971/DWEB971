/*  tableau des apprenants */
const apprenants = ["POULIER", "MUSQUET", "HENRI", "LAKE", "ANTOINE", "CICERON", "ELICE", "ELELOUE", "SYDNEY", "DAUPIN", "BIANAY"];
let index = Math.floor(Math.random() * apprenants.length);

alert(index);